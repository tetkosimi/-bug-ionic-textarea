import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bug-ionic-textarea',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
